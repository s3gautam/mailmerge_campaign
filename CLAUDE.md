# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty (no source files, package manifests, or configuration beyond `.git`). There is no build, lint, or test tooling to document yet.

When code is added to this repository, update this file with:
1. Commands for building, linting, and running tests (including how to run a single test).
2. A high-level description of the code architecture and structure.
